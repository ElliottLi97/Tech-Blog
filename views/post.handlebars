<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
    <button class="btn btn-sm btn-danger" data-id="{{post.id}}">ADD A COMMENT</button>
  </p>
</div>
