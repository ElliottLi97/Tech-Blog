1. Add user comment data to posts /post/:id via homeRoutes.js
2. Create a model that is associated wtih a post and a user 
3. On a a post if the user is the original poster then render a button that allows them to edit their post